# F45 · Policy Registry

Version: `policy-registry-v0.1`.

Creates immutable version records from explicit F44 approvals and optional previous-version rollback references. Registration is not activation: no runtime config is written and the active policy is unchanged.
