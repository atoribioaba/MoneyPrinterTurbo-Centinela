# F42 · Policy Simulator

Version: `policy-simulator-v0.1`.

Runs baseline and candidate through the real deterministic `CinematicDirector().build(...)` on supplied planning cases. Compares direction hashes, tension curve, climax, structural checks and placeholder preservation. No render, GPU, runtime-config write or activation.
